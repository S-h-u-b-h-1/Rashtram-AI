NEXT_PUBLIC_API_URL=http://localhost:5001/api
NEXT_PUBLIC_FORMSPREE_CONTACT_ENDPOINT=
# Optional build-time fallback used only if the server capability check fails.
# The server remains authoritative and requires GOOGLE_CLIENT_ID/SECRET.
NEXT_PUBLIC_GOOGLE_AUTH_ENABLED=false
